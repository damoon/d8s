apiVersion: v1
kind: Service
metadata:
  name: docker.io-mirror
spec:
  ports:
    - name: http
      port: 5000
  selector:
    app: docker.io-mirror
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: docker.io-mirror
spec:
  selector:
    matchLabels:
        app: docker.io-mirror
  strategy:
    type: Recreate
  replicas: 1
  template:
    metadata:
      labels:
        app: docker.io-mirror
    spec:
      containers:
        - name: docker.io-mirror
          image: registry:2.7.1
          ports:
            - name: http
              containerPort: 5000
          resources:
            limits:
              cpu: "100m"
              memory: "500Mi"
            requests:
              cpu: "100m"
              memory: "500Mi"
          readinessProbe:
            httpGet:
              path: /v2/
              port: 5000
          lifecycle:
            preStop:
              exec:
                command:
                  - sh
                  - -c
                  - "sleep 10"
          volumeMounts:
            - name: data
              mountPath: /var/lib/registry
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: docker.io-mirror
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docker.io-mirror
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
