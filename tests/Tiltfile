
local_resource ('test docker build',
  'timeout 60 bash docker-build.sh',
  deps=['..'],
  resource_deps=['wedding'],
  allow_parallel=True,
)

local_resource ('test docker pull tag push',
  'timeout 100 bash docker-pull-tag-push.sh',
  deps=['..'],
  resource_deps=['wedding'],
  allow_parallel=True,
)

local_resource ('test docker inspect',
  'timeout 100 bash docker-inspect.sh',
  deps=['..'],
  resource_deps=['wedding'],
  allow_parallel=True,
)

local_resource ('test tilt ci',
  'timeout 170 bash tilt-ci.sh',
  deps=['..'],
  resource_deps=['wedding'],
  allow_parallel=True,
)
